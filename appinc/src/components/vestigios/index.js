import React from 'react';
import { View, Text } from 'react-native';

import { Camera, InputText, Scanner } from '../../pages/components/';

const Vestigios = () => (
    
    <View>
        <Camera />
        <InputText />
        <Scanner />
    </View>
  );

export default Vestigios;